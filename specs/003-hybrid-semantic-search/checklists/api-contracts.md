# API Contracts Checklist: Hybrid Semantic Search

**Purpose**: Requirements-quality validation ("unit tests for English") for the
API-contract surfaces of SPEC-003 — the library `searchNodes` mode plumbing, the
`codegraph_search` MCP tool schema, the CLI `query` mode flag, the `SearchResult`
provenance/score shape, the FR-017 `codegraph status` availability line, and the
provable non-regression of internal `searchNodes` callers.
**Created**: 2026-07-09
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [contracts/search-api.md](../contracts/search-api.md) · [contracts/mcp-cli-surface.md](../contracts/mcp-cli-surface.md) · [contracts/degradation-hints.md](../contracts/degradation-hints.md)

**Note**: Generated by `/speckit-checklist api-contracts`. Each item tests whether an
API-contract requirement is present, clear, measurable, consistent, and complete —
NOT whether the implementation satisfies it.

## Mode Parameter — Completeness Across Surfaces

- [ ] CHK001 Are the accepted `mode` values (`keyword|semantic|hybrid|auto`) and the library default (`keyword`) explicitly specified for `searchNodes`? [Completeness, Spec §FR-001, contracts/search-api]
- [ ] CHK002 Is the surface-level default (`auto`) for both `codegraph_search` and CLI `query` specified distinctly from the library default (`keyword`)? [Completeness/Consistency, Spec §FR-002, contracts/mcp-cli-surface]
- [ ] CHK003 Is the `mode` property specified as OPTIONAL and additive (backward-compatible, no `required` change) on the `codegraph_search` MCP `inputSchema`? [Completeness, contracts/mcp-cli-surface]
- [ ] CHK004 Is the CLI `-m, --mode` flag's accepted value set and unspecified→`auto` default specified? [Completeness, contracts/mcp-cli-surface]
- [x] CHK005 Is the CLI `-m, --mode` help/description text content specified (the human-readable option description enumerating the modes and stating the `auto` default)? [Resolved →FR-002] (help string `Search mode: keyword | semantic | hybrid | auto (default: auto)` added to FR-002 + contracts/mcp-cli-surface)
- [ ] CHK006 Is it specified that auto-resolution lives at the surfaces (a shared helper), NOT in the library `searchNodes`, so the library default stays `keyword`? [Clarity, Spec §FR-002/FR-003, contracts/search-api]

## Backward Compatibility & Result-Shape Stability

- [ ] CHK007 Are `matchType` and `fusedScore` specified as OPTIONAL and ABSENT (not `undefined`) in keyword mode so existing shapes stay byte-identical? [Completeness, Spec §FR-012, contracts/search-api]
- [ ] CHK008 Is byte-identical keyword-mode output asserted across all three surfaces (library, MCP, CLI human + `--json`)? [Consistency, Spec §SC-004/FR-008/FR-012]
- [x] CHK009 Does the spec define the value and semantics of the existing non-optional `score` field in semantic/hybrid modes (what it holds for a `semantic`-only hit that carries no keyword BM25 magnitude, and its relationship to `fusedScore` and to the rendered order)? [Resolved →FR-012] [Ambiguity] (FR-012 + contracts/search-api "`score` value by mode": semantic/hybrid `score` = `fusedScore`, keyword unchanged; flagged for consensus — affects existing `--json` consumers)
- [ ] CHK010 Is the change of the `codegraph_search`/CLI default from today's keyword behavior to `auto` acknowledged as intended, and reconciled with SC-004 (which scopes byte-identity to the library default and internal callers, not the surface default)? [Consistency, Spec §FR-002/SC-004]
- [ ] CHK011 Is the CLI `--json` added-field behavior (matchType, fusedScore, embedMs, fusionMs present only when the semantic arm ran; absent otherwise) specified? [Completeness, Spec §FR-008/FR-012, contracts/mcp-cli-surface]

## Mode Semantics & Invalid-Input Handling

- [ ] CHK012 Is `semantic` mode's vector-arm-only semantics (no FTS/exact-name supplement; MAY omit an exact-name-only symbol) specified? [Clarity, Spec §FR-002a]
- [ ] CHK013 Is `hybrid` fusion specified precisely (rank-only RRF `k=60`, per-arm depth `max(5×limit,100)`, order by fused score desc, truncate to `limit`)? [Clarity, Spec §FR-004/FR-004a]
- [x] CHK014 Is the handling of an unknown/invalid `mode` value specified CONSISTENTLY across all three surfaces (library, MCP, CLI)? [Resolved →FR-001/FR-002] (library coerces unknown→`keyword` default; MCP + CLI coerce unknown→`auto`; never error, no commander `choices()`; added to FR-001, FR-002 + both contracts)
- [x] CHK015 Is the interaction of `options.offset` (pagination) with the fused, `limit`-truncated result set specified for semantic/hybrid modes? [Resolved →FR-012] (FR-012 + contracts/search-api: fused union sliced `[offset, offset+limit)` after fusion, identical to keyword pagination; offset is library-only, not exposed by MCP/CLI)
- [ ] CHK016 Is the `auto` predicate (hybrid iff provider configured AND ≥1 matching-model vector, else keyword) specified identically for the surfaces and the FR-017 status line? [Consistency, Spec §FR-002/FR-017, contracts/mcp-cli-surface]

## Provenance & Timing Annotation

- [ ] CHK017 Is the `matchType` enum (`keyword|semantic|both`) and its meaning (which arm(s) contributed a rank to the fused score) specified? [Clarity, Spec §FR-012, Key Entities]
- [ ] CHK018 Are the inline bracket-tag rendering (`[keyword]`/`[semantic]`/`[both]`) and its restriction to semantic/hybrid modes specified for BOTH the MCP and CLI surfaces? [Completeness, Spec §FR-012, contracts/mcp-cli-surface]
- [ ] CHK019 Is the fused-score visibility rule unambiguous (CLI `--json` only; never in human-readable or MCP output)? [Clarity, Spec §FR-012/FR-008]
- [ ] CHK020 Is the timing footer (`semantic: embed …ms · fusion …ms`) rendering, plus its `--json` machine fields and its omission in keyword/degraded output, specified for both surfaces? [Completeness, Spec §FR-008, contracts/mcp-cli-surface]

## Status Availability Contract (FR-017)

- [ ] CHK021 Is the `codegraph status` "Hybrid search available" line's yes/no derivation predicate specified and aligned with the FR-002 auto predicate? [Consistency, Spec §FR-017/SC-007, contracts/degradation-hints]
- [x] CHK022 Is the field name and shape of the availability line in `--json` status output specified? [Resolved →FR-017] (FR-017 + contracts/degradation-hints: additive `hybridSearchAvailable: boolean` + `hybridSearchReason: string|null`; existing status --json shape byte-stable — flagged for consensus on exact field naming)
- [ ] CHK023 Are the `no (...)` reason strings drawn verbatim from the same success-shaped degradation-hint vocabulary specified? [Clarity, Spec §FR-017, contracts/degradation-hints]
- [ ] CHK024 Is it specified that the line derives SOLELY from the existing `getEmbeddingStatus` snapshot (no new probe; must not report live per-daemon provider warmth)? [Completeness, Spec §FR-017]

## Internal-Caller Non-Regression (FR-003/FR-003a)

- [ ] CHK025 Is the requirement that internal callers (explore, prompt hook, context builder) receive byte-identical keyword results with no result-shape change specified? [Completeness, Spec §FR-003, contracts/search-api]
- [ ] CHK026 Is there a MEASURABLE assertion that internal callers make ZERO query-embed calls (provider-seam spy) — the proxy for the latency invariant? [Measurability, Spec §FR-003a/FR-014, contracts/search-api]
- [ ] CHK027 Is the internal-caller guarantee specified as BOTH output-identical (SC-004) AND latency-non-regressed (no matrix build, no staleness probe, no query-embed)? [Consistency, Spec §FR-003a]

## Filters, Determinism & Traceability

- [ ] CHK028 Are `kind:`/`lang:`/`options.kinds` (pre-filter before top-k) vs `path:`/`name:` (post-fusion hard gate) specified with identical semantics across keyword, semantic, and hybrid modes? [Consistency, Spec §FR-010/FR-016, contracts/search-api]
- [ ] CHK029 Is deterministic ordering (stable node-id tie-break applied at BOTH the per-arm and fused levels) specified for identical query + index? [Measurability, Spec §FR-013/SC-006]
- [ ] CHK030 Are the MCP tool name (`codegraph_search`) and CLI command (`codegraph query`) referenced consistently between spec.md and the contract files, matching the actual exposed surfaces? [Consistency, Traceability, contracts/mcp-cli-surface]

## Notes

- Check items off as completed: `[x]`
- A `Gap`-tagged item (bracketed) marks a missing/ambiguous requirement to be remediated or escalated to consensus.
- Resolved gaps are re-marked inline as `[Resolved →FR-###]` (mirrors the `performance.md` convention).
