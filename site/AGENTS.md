# Site - Local Rules

Full detail: root `AGENTS.md`.

- This is a separate Astro/Starlight documentation site.
- Keep docs user-facing and current with CLI behavior; avoid implementation
  jargon unless the page is explicitly a design reference.
- Do not duplicate agent tool guidance from `src/mcp/server-instructions.ts`.
- Validate site changes with the local site commands from `site/package.json`.
