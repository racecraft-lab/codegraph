# Load local, untracked CodeGraph/HAL settings when present.
if command -v source_env_if_exists >/dev/null 2>&1; then
  source_env_if_exists .envrc.local
elif [ -f .envrc.local ]; then
  source ./.envrc.local
fi
